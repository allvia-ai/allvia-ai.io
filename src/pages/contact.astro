---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Section from '../components/ui/Section.astro';
import Container from '../components/ui/Container.astro';
import Button from '../components/ui/Button.astro';
import Card from '../components/ui/Card.astro';

const contactChannels = [
  {
    title: '일반 이메일',
    value: 'steer@nova-allrounder.io',
    href: 'mailto:steer@nova-allrounder.io',
    id: 'email',
    note: '제품 문의, 피드백 등 일반적인 연락에 사용하세요.',
  },
  {
    title: 'GitHub',
    value: 'github.com/allvia-ai',
    href: 'https://github.com/allvia-ai',
    id: 'github',
    note: '오픈소스 저장소 및 이슈 트래커.',
  },
  {
    title: '영업 문의',
    value: 'steer@nova-allrounder.io',
    href: 'mailto:steer@nova-allrounder.io',
    id: 'sales',
    note: '데모, 파트너십, Enterprise 상담에 사용하세요.',
  },
];

const teamOverview = [
  { label: '핵심 팀', value: '4명', note: '제품, 엔지니어링, 디자인, 성장' },
  { label: '트랙', value: 'B2C + B2B', note: '공유 플랫폼, 별도 GTM 전략' },
  { label: '시간대', value: 'KST + 원격', note: '일일 비동기 + 고정 동기 시간' },
];

const teamProfiles = [
  {
    name: 'David Park',
    role: 'PM, Developer',
    focus: '기획, 개발',
    email: 'qed4950@gmail.com',
    github: 'github.com/qed4950-web',
    photoAlt: '프로필 A',
  },
  {
    name: 'Juyn Lee',
    role: 'Developer',
    focus: '개발',
    email: 'juyeon2yo@gmail.com',
    github: 'github.com/juyn-lee',
    photoAlt: '프로필 B',
  },
  {
    name: 'Kyukyung Lee',
    role: 'Developer',
    focus: '개발',
    email: 'dlrbrud0410@gmail.com',
    github: 'github.com/kyuuu410',
    photoAlt: '프로필 C',
  },
  {
    name: 'Jiwon Son',
    role: 'Developer',
    focus: '개발',
    email: 'jennysiw715557@gmail.com',
    github: 'github.com/zioni715',
    photoAlt: '프로필 D',
  },
];
---

<BaseLayout title="문의 - AllvIa" description="이메일, GitHub, 팀 프로필 등 연락 정보 허브." lang="ko">
  <Header slot="header" lang="ko" />

  <Section padding="large">
    <Container>
      <div class="text-center max-w-4xl mx-auto animate-on-scroll">
        <p class="text-caption text-[#a5b4fc] font-medium tracking-wide uppercase mb-4">문의</p>
        <h1 class="text-h1 md:text-display text-neutral-100 mb-6 text-balance">연락에 필요한 모든 정보를 한 곳에</h1>
        <p class="text-body-lg text-neutral-400 max-w-2xl mx-auto mb-10">
          팀 연락처와 멤버 프로필을 확인하세요.
        </p>
        <Button variant="primary" size="lg" href="#contact-list">연락처 보기</Button>
      </div>
    </Container>
  </Section>

  <Section id="contact-list" background="light">
    <Container>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 animate-on-scroll">
        {contactChannels.map((item) => (
          <Card hoverable={false}>
            <div class="p-6" id={item.id}>
              <p class="text-caption text-[#a5b4fc] uppercase tracking-wide mb-2">{item.title}</p>
              <a
                href={item.href}
                target={item.href.startsWith('http') ? '_blank' : undefined}
                rel={item.href.startsWith('http') ? 'noopener noreferrer' : undefined}
                class="text-h4 text-neutral-100 break-all hover:text-accent-200 transition-colors"
              >
                {item.value}
              </a>
              <p class="text-caption text-neutral-400 mt-3">{item.note}</p>
            </div>
          </Card>
        ))}
      </div>
    </Container>
  </Section>

  <Section>
    <Container>
      <div class="text-center mb-12 animate-on-scroll">
        <h2 class="text-h2 text-neutral-100 mb-4">팀 개요</h2>
        <p class="text-body-lg text-neutral-400">AllvIa를 만드는 팀 구성과 운영 방식.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12 animate-on-scroll">
        {teamOverview.map((item) => (
          <Card hoverable={false}>
            <div class="p-6">
              <p class="text-caption text-[#a5b4fc] uppercase tracking-wide mb-2">{item.label}</p>
              <p class="text-h3 text-neutral-100 mb-2">{item.value}</p>
              <p class="text-caption text-neutral-400">{item.note}</p>
            </div>
          </Card>
        ))}
      </div>

      <div class="text-center mb-12 animate-on-scroll">
        <h2 class="text-h2 text-neutral-100 mb-4">팀 멤버</h2>
        <p class="text-body-lg text-neutral-400">4명의 멤버 이름, 사진, 이메일, GitHub 링크를 추가하세요.</p>
      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 animate-on-scroll">
        {teamProfiles.map((person) => (
          <Card hoverable={false}>
            <div class="p-6 text-center h-full">
              <div class="w-28 h-28 rounded-full border border-white/10 bg-[#101217] mx-auto mb-4 flex items-center justify-center">
                <span class="text-xs text-neutral-400">사진</span>
              </div>
              <h3 class="text-h4 text-neutral-100">{person.name}</h3>
              <p class="text-caption text-neutral-400 mt-1">{person.role}</p>
              <p class="text-caption text-neutral-500 mt-3">{person.focus}</p>
              <div class="mt-5 space-y-1">
                <a href={`mailto:${person.email}`} class="block text-xs text-[#a5b4fc] hover:text-[#c7d2fe] transition-colors break-all">
                  {person.email}
                </a>
                <a href={`https://${person.github}`} target="_blank" rel="noopener noreferrer" class="block text-xs text-neutral-400 hover:text-neutral-200 transition-colors break-all">
                  {person.github}
                </a>
              </div>
              <p class="sr-only">{person.photoAlt}</p>
            </div>
          </Card>
        ))}
      </div>
    </Container>
  </Section>

  <Section background="dark">
    <Container narrow>
      <div class="text-center animate-on-scroll">
        <h2 class="text-h2 text-white mb-4">간단한 소개 통화가 필요하신가요?</h2>
        <p class="text-body-lg text-neutral-400 mb-8">
          아래 버튼으로 이메일을 보내시거나, 일정 링크를 추가해주세요.
        </p>
        <Button variant="primary" size="lg" href="mailto:hello@nova-platform.io">이메일 보내기</Button>
      </div>
    </Container>
  </Section>

  <Footer slot="footer" />
</BaseLayout>
