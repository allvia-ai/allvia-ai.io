---
import { t, type Locale } from '../i18n/utils';

interface Props {
  lang?: Locale;
}
const { lang = 'ko' } = Astro.props;

const currentYear = new Date().getFullYear();
const repo = process.env.GITHUB_REPOSITORY?.split('/')[1] ?? '';
const isUserSiteRepo = repo.endsWith('.github.io');
const basePath = process.env.GITHUB_ACTIONS === 'true'
  ? (isUserSiteRepo ? '/' : `/${repo}/`)
  : '/';

const withBase = (url: string) => {
  if (
    url.startsWith('http://') ||
    url.startsWith('https://') ||
    url.startsWith('mailto:') ||
    url.startsWith('tel:') ||
    url.startsWith('#')
  ) {
    return url;
  }
  if (!url.startsWith('/')) return url;

  const clean = url.replace(/^\/+/, '');
  return clean ? `${basePath}${clean}` : basePath;
};

const localizeHref = (href: string) => {
  if (href.startsWith('http') || href.startsWith('#')) return href;
  if (lang === 'en') {
    return withBase(`/en${href}`);
  }
  return withBase(href);
};

const footerLinks = {
  [t(lang, 'footer.product')]: [
    { label: t(lang, 'footer.product.overview'), href: '/product' },
    { label: t(lang, 'footer.product.pricing'), href: '/pricing' },
    { label: t(lang, 'footer.product.updates'), href: '/updates' },
  ],
  [t(lang, 'footer.resources')]: [
    { label: t(lang, 'footer.resources.docs'), href: '/docs' },
    { label: t(lang, 'footer.resources.getting-started'), href: '/docs/getting-started' },
    { label: t(lang, 'footer.resources.api'), href: '/docs/api' },
    { label: t(lang, 'footer.resources.faq'), href: '/docs/faq' },
  ],
  [t(lang, 'footer.contact')]: [
    { label: t(lang, 'footer.contact.hub'), href: '/contact' },
    { label: t(lang, 'footer.contact.email'), href: '/contact#email' },
    { label: t(lang, 'footer.contact.github'), href: '/contact#github' },
  ],
  [t(lang, 'footer.legal')]: [
    { label: t(lang, 'footer.legal.terms'), href: '#' },
    { label: t(lang, 'footer.legal.privacy'), href: '#' },
    { label: t(lang, 'footer.legal.license'), href: '#' },
  ],
};

const copyrightText = t(lang, 'footer.copyright').replace('{year}', String(currentYear));

---

<footer class="bg-[#0d0e11] border-t border-white/10" role="contentinfo">
  <div class="max-w-container mx-auto px-6 py-14">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-8 lg:gap-10">
      {Object.entries(footerLinks).map(([category, links]) => (
        <div>
          <h3 class="text-xs font-semibold text-neutral-300 tracking-[0.08em] uppercase mb-4">{category}</h3>
          <ul class="space-y-2.5" role="list">
            {links.map((link) => (
              <li>
                <a href={localizeHref(link.href)} class="text-sm text-neutral-500 hover:text-neutral-100 transition-colors">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>

    <div class="mt-14 pt-7 border-t border-white/10 flex flex-col md:flex-row items-center justify-between gap-4">
      <div class="flex items-center gap-2 text-neutral-100 font-semibold text-sm tracking-[0.08em] uppercase">
        <span class="inline-flex items-center justify-center w-6 h-6 bg-[#a5b4fc] text-[#111214] rounded text-[10px] font-bold">A</span>
        <span>AllvIa</span>
      </div>
      <p class="text-xs text-neutral-500">{copyrightText}</p>
      <div class="flex items-center gap-4">
        <a
          href="https://github.com/allvia-ai/allvia-ai.io"
          target="_blank"
          rel="noopener noreferrer"
          class="text-neutral-500 hover:text-neutral-100 transition-colors"
          aria-label="GitHub"
        >
          <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd" />
          </svg>
        </a>
      </div>
    </div>
  </div>
</footer>
